---
paths:
  - 'app/Services/WeeklyReport*.php,app/Jobs/SendWeeklyRankingReport.php,resources/views/components/weekly-overview.blade.php'
---

# Components

## Weekly Overview is one saved snapshot shared by dashboard and existing email
Generate WeeklyReport inside the existing weekly ranking email process, using its frozen dispatch date; never add a second email schedule or generate on dashboard requests. Reuse the persisted narrative and evidence on both surfaces, including retries. Search Console compares dated, lagged whole weeks and suppresses incomplete data; missing observations and omitted samples are not zero or losses. Audit resolution requires a newly passing matching check. AI only explains bounded computed facts; priorities retain deterministic source evidence. Keep future sources optional and omit unavailable AI visibility.
