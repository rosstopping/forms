---
paths:
  - 'app/{Http/Controllers/Admin,Services,Jobs}/**/*SeoProspect*.php'
---

# Admin Services Jobs

## SEO candidate imports remain manual and evidence-backed
Import only manually selected suitable candidates. Lock the search/candidates, deduplicate prospects by normalized domain, attach the stored SEO evidence to a prospect activity, and dispatch analysis after commit. Import must never send outreach; approval remains mandatory.
