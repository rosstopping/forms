---
paths:
  - 'app/{Jobs,Services/SeoIntelligence,Http/Controllers/Admin}/**/*Seo*.php,app/Models/Website.php'
---

# Admin Models

## Keep paid SEO snapshots opt-in
Automatic DataForSEO weekly snapshots must remain disabled by default and controlled per website. Enabling queues one unique historical backfill; disabling stops future paid snapshots without deleting stored history. Keep DataForSEO estimates clearly separate from first-party Search Console metrics.
